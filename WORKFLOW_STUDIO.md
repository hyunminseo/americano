# Americano Workflow Studio

새 기본 화면은 `electron/workflow.html`입니다. 기존 선형 편집기는 유지했지만 기본 실행 경로에서는 사용하지 않습니다. 기존 version 2 매크로를 열면 각 액션을 연결된 노드로 변환하며, 조건/반복/재시도 내부 데이터는 그대로 보존합니다. 기존 복합 액션은 노드 설정의 JSON 편집으로 수정할 수 있습니다.

## 사용

1. 워크플로우를 만들고 배경을 클릭해 대상 창을 선택합니다.
2. 작성 기준을 800×600 또는 1280×960으로 정합니다.
3. macOS의 제목 표시줄 등 게임 외부 영역이 있으면 **콘텐츠 영역**에 창 내부 x/y/너비/높이를 설정합니다. Windows 기본 영역은 클라이언트 영역, macOS 기본 영역은 네이티브 창 영역입니다. 콘텐츠 영역 입력값은 Windows에서는 물리 픽셀, macOS에서는 화면 포인트입니다.
4. 노드 라이브러리에서 액션을 추가합니다. 출력 점을 클릭한 다음 대상 노드의 입력 점을 클릭합니다. 조건 노드는 있음/없음 두 경로를 연결합니다. 연결선 클릭으로 연결을 삭제합니다.
5. 이미지 노드에서 파일을 선택하거나 화면을 캡처합니다. 기존 파일은 **이미지를 캡처한 해상도**도 지정합니다. 파일에서 해상도를 추측하지 않습니다.
6. 저장하고 흐름 미리보기로 연결을 확인한 뒤 실행합니다. 미리보기는 실제 화면 인식과 입력을 하지 않으며 조건의 없음 경로를 사용합니다.
7. F7 시작, F8 일시정지/재개, F9 중지. 실행 중에는 편집을 잠급니다. 실행 기록은 현재/최근 실행의 최대 200개 노드 방문을 표시합니다.

키 조합 예: `enter`, `ctrl+a`, `shift+f1`. macOS Command는 `win`으로 입력합니다. 전체 워크플로우 반복 횟수와 반복 간격은 설정 패널에 있습니다. 무제한 순환 연결은 허용하지 않습니다.

## 해상도와 인식

- `reference`가 있는 워크플로우는 창 크기/DPI와 독립적인 기준 픽셀을 사용합니다.
- 캡처 이미지를 기준 해상도로 정규화하고, 기준 이미지의 `reference_width`가 다르면 함께 변환합니다. 클릭에는 동일한 변환을 역으로 적용합니다.
- 800×600 ↔ 1280×960의 1.6배 확대/축소와 동일한 4:3 콘텐츠를 지원합니다. UI가 재배치되거나 개별 요소의 크기가 독립적으로 고정된 게임까지 동일 이미지를 보장하지는 않습니다.
- 콘텐츠의 종횡비가 4:3에서 1.5% 이상 달라지면 실행 중 명확한 오류로 중단합니다. 검은 여백을 자동 추측하지 않습니다.
- 기존 매크로는 DPI 좌표를 유지합니다. 기준 해상도로 전환할 때 기존 이미지·좌표가 그 기준에서 작성되었는지 검토해야 합니다.
- 홈 위치/구역/전체 탐색의 반환 좌표는 모두 검색 영역 상대 좌표입니다. 축소 후보는 원본 해상도에서 검증해야 성공입니다.

## 데이터와 플랫폼

기존 암호화 저장소, 라이선스 확인, 이미지 패키지 형식은 유지합니다. workflow 그래프가 존재하면 그래프가 실행의 기준이며 actions는 호환용으로 생성됩니다. 가져온 워크플로우는 로컬 대상 창을 재선택하고 검토 플래그를 해제해야 실행할 수 있습니다. 개발 모드 데이터는 프로젝트의 `.runtime/`에 저장합니다. 패키징된 앱의 저장 위치는 기존 Electron userData입니다.

Windows는 Win32 입력/클라이언트 창 API를 사용합니다. macOS는 nut-js/libnut 네이티브 창·마우스·키보드 API를 사용하며, Win32 DLL을 로드하지 않습니다. macOS는 시스템 설정 → 개인정보 보호 및 보안의 **손쉬운 사용**과 **화면 기록** 권한이 필요합니다. 권한이 없으면 실제 실행을 중단하고 안내합니다.

## 개발 및 빌드

Node.js 24 / npm 11:

```sh
npm ci
npm test
npm start
npm run build:win
npm run build:mac
```

Windows 빌드는 Windows, macOS 빌드는 macOS에서 실행하세요. macOS 명령은 arm64/x64 DMG를 생성합니다. 배포용 Developer ID 서명·공증과 Windows 코드 서명은 별도로 구성해야 합니다.

이 작업 환경에서 확인한 내용: 기존 회귀 테스트, 그래프 검증/분기/취소/미리보기, 4개 해상도 조합의 합성 이미지 인식과 클릭 좌표, 기준 이미지 배율 변경, DOM 기반 편집/연결/되돌리기/저장 복원. Chrome GUI 검증은 실행 환경 오류로 완료하지 못했습니다. Windows/macOS 실제 게임 화면에서의 캡처·전경 전환·Retina·권한·입력 및 설치 패키지는 실기기 검증이 필요합니다.
