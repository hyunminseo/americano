Add one PNG per fixed allowlisted phrase before building the application:

  니플레티.png

Each image must contain only the phrase as it appears in the center of the
target application. These files are required at runtime and are not read from
config.json. Text recognition engines are not used.
