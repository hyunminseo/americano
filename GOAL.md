# 현재 구현 목표

이 문서는 활성 goal과 이후 사용자가 추가한 완료 조건을 함께 추적한다.

- [x] 워크플로우 노드 연결·이동·중첩 편집 UI
- [x] 기존 actions 및 저장된 매크로 호환
- [x] 캡처 이미지를 포함하는 매크로 import/export
- [x] import 후 대상 창 확인 및 오버레이 재지정으로 실행
- [x] 좌표 기준 구분 및 크기 차이에 대한 보정 안내
- [x] 워크플로우·패키지 테스트, 빌드, MACRO_DESIGN.md 동작 순서 기록 (실제 입력 확장 검증은 아래 별도 추적)

## 추가 완료 조건: 대상 프로그램 키보드 입력

사용자 추가 요청: 탐지한 프로그램에 키보드 입력이 제대로 되는지 검증하고 구현한다.

- [x] 대상 창을 확인하고 활성화한 다음 입력하는 경로 점검 및 수정
- [x] 단일 키, 특수 키, 조합 키, 한글을 포함한 문자 입력 구현 검증
- [x] 테스트용 수신 프로그램에서 실제 Windows 키보드 이벤트와 입력 문자열 확인
- [x] 다른 창에 입력하지 않는지, 대상 소실·활성화 실패 시 중단되는지 검증
- [x] F8 일시정지·재개, F9 중지 후 추가 입력 차단 및 누른 키 해제 검증
- [x] 자동 검증 결과와 실제 대상 앱별 제한 사항 문서화

기본 goal 완료 여부를 판단할 때 이 추가 조건도 포함한다.

## 추가 완료 조건: 마우스 자동 재현

사용자 요청에 따라 테스트 창의 활성화 클릭도 프로그램이 수행한다. 수동 클릭을 요구하는 테스트 모드는 제거했다.

- [x] 대상 창이 클릭 지점에 있는지 확인하고 이동 실패·가려짐·전경 변경 시 클릭 차단
- [x] 실제 Windows 좌표 이동, 좌표 클릭 및 이미지 탐지 후 클릭 수신 검증
- [x] 마우스로 테스트 창을 활성화한 뒤 확장 키보드 테스트까지 자동 완료

이전 실행에서 관찰한 환경 제한: Windows `SetCursorPos`는 같은 위치로의 이동에도 false를 반환했다. 별도 C# P/Invoke에서도 동일했다. `SendInput` 이동은 접수 개수를 반환했지만 실제 커서는 요청 좌표로 이동하지 않았다. 당시 검증은 이를 감지하여 클릭 전에 중단했다. 이후 동일 코드의 자동 수신 테스트가 통과했다. 이전 실패 원인을 라이브러리의 좌표 계산 오류로 단정하지 않는다.

## 검증 기록

- `npm test`: 61개 통과.
- `npm run test:overlay-ui`: 캡처·저장, 워크플로우 순서 변경, 패키지 왕복, 영역 재지정, 재시작 복원 통과.
- 키보드 기본 수신 테스트: 실제 화면 이미지 탐지 후 Ctrl+A, 한글·영문·이모지, Enter, 단일 키, F6 수신 및 일시정지·중지 확인.
- `npm run test:keyboard-ui`: 자동 마우스 활성화, 좌표 클릭, 이미지 클릭, 실제 이미지 탐지, 한글·이모지·조합 키·특수 키, F8 일시정지·재개, F9 중지, 전경 변경·활성화 실패·대상 소실 시 입력 차단, 키 해제 검증까지 통과했다. 입력 수신 창의 좌표·문자·이벤트를 검사했으며 테스트 실행 권한만 테스트 내부에서 제공한다.
- 마우스 변경을 반영한 `npm run build` 및 패키지 내 UI·네이티브 모듈·암호화 저장 로딩 검증 통과.
