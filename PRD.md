# Americano Product Requirements Document

## 1. 제품 정의

Americano는 특정 Windows 앱 창을 대상으로 하는 매크로 자동화 데스크톱 도구다. 사용자가 매크로 순서를 UI에서 구성하고, 대상 창 안에서 이미지를 탐지한 뒤 지정 시간 대기와 키보드/마우스 입력을 순서대로 실행한다.

앱은 Node.js 24 기반 Electron 하나로 동작한다. 별도 서버, Python, 컨테이너, AutoHotkey 실행 파일이나 스크립트 엔진은 사용하지 않는다. AutoHotkey는 사용자 경험과 기능 범위를 참고하는 대상일 뿐이며, 모든 창 탐색, 캡처, 이미지 매칭, 대기, 입력 전송, 매크로 실행은 Electron main process와 Node.js native module로 직접 구현한다.

## 2. 목표와 비목표

### 목표

- 특정 앱 창을 선택해 매크로의 실행 대상으로 지정한다.
- 여러 매크로를 등록하고 각각 독립적으로 실행한다.
- 이미지 탐지 후 일정 시간 sleep하고 키보드/마우스 입력을 실행한다.
- 창 기준 상대 좌표를 사용해 창 이동과 크기 변경에 대응한다.
- 해상도와 DPI 배율이 달라져도 가능한 범위에서 매크로가 동작한다.
- UI에서 AutoHotkey 스타일의 순서 편집기로 매크로를 구성한다.
- 매크로와 기준 이미지를 앱 내부에 저장하고 import/export한다.
- 인터넷 없이 서명된 라이선스를 검증한다.
- 관리자 권한으로 실행한다.
- 프로그램 전체에서 한 번에 하나의 매크로만 실행한다.
- F8 일시정지와 F9 긴급 정지를 제공한다.

### 비목표

- OCR, 음성 명령, 얼굴/객체 인식은 1차 버전에 포함하지 않는다.
- 일반 키보드/마우스 이벤트 기록은 1차 범위에 포함하지 않는다. 필요한 것은 매크로 시작 단축키 감지다.
- 임의의 시스템 명령 실행이나 원격 코드 실행은 지원하지 않는다.
- AutoHotkey 프로세스, 스크립트, COM 연동은 지원하지 않는다.
- macOS와 Linux는 1차 지원 대상이 아니다.

## 3. 플랫폼과 기술

- 운영체제: Windows 10 이상
- 런타임: Node.js 24.x, npm 11+
- 데스크톱 셸: Electron
- 키보드/마우스 입력: `@nut-tree-fork/nut-js`
- 화면 캡처: `screenshot-desktop`
- 이미지 처리: `sharp`
- 창 탐색/활성화/좌표 변환: Windows API를 사용하는 Node.js native addon 도입 예정
- 배포 형식: `electron-builder` Windows x64 portable 단일 EXE, 설치 과정 없음

`package.json`은 Node.js major 버전을 24로 제한한다. Electron renderer는 운영체제 API에 직접 접근하지 않고 preload IPC를 사용한다. native module은 Electron ABI에 맞춰 rebuild하고 Windows 패키지에서 로드 여부를 검증한다.

관리자 권한으로 실행된 대상 앱을 지원한다. 일반 권한 Electron 앱이 관리자 권한 창에 입력할 수 없는 경우에는 앱을 관리자 권한으로 실행하거나 필요한 권한 상승을 안내한다. 권한 상승은 사용자 동의 없이 수행하지 않는다.

## 4. 사용자 흐름

1. 포터블 EXE를 실행한다. 신규 사용자에게 첫 매크로 만들기 안내를 표시하며 기본 매크로를 자동 등록하지 않는다.
2. 매크로를 새로 만들거나 export 패키지를 import한다.
3. 실행 대상 앱 창을 선택한다.
4. 시작 단축키와 이미지 탐지, 대기, 키 입력, 클릭 단계를 순서대로 구성한다.
5. 매크로를 앱 내부 저장소에 저장하고 활성화한다.
6. UI 실행 버튼 또는 매크로 단축키로 매크로를 실행한다.
7. UI에서 현재 매크로, 현재 단계, 실행 횟수, 최근 로그를 확인한다.
8. F8로 일시정지하고 F9 또는 중지 버튼으로 즉시 정지한다.

## 5. 기능 요구사항

### 5.1 매크로 관리

- 여러 매크로의 생성, 수정, 복제, 삭제, 활성화/비활성화를 지원한다.
- 매크로마다 ID, 이름, 설명, 대상 창 조건, 시작 단축키, 순서가 있는 단계 목록을 가진다.
- 매크로는 앱 내부 데이터 폴더에 저장한다.
- 매크로와 기준 이미지를 하나의 import/export 패키지로 내보내고 가져온다.
- 매크로와 기준 이미지는 앱 내부 사용자 데이터 폴더에 고정해 암호화 저장한다.
- import 시 파일과 이미지 참조를 임시 폴더에서 검증한 뒤 성공할 때만 저장소에 반영한다.
- 기존 ID가 충돌하면 새 ID 생성, 덮어쓰기, 취소를 사용자가 선택한다.
- 동일한 시작 단축키를 여러 활성 매크로에 중복 등록할 수 없다.
- 매크로는 전체 실행, 일시정지, 중지, 재시작이 가능하다.
- 프로그램 전체에 단일 실행 잠금을 적용한다. 다른 매크로의 실행 요청은 대기열에 넣지 않고 거부한다.

### 5.2 대상 창

- 사용자가 실행 대상 앱 창을 목록에서 선택한다.
- 창 제목, 프로세스 이름, 실행 파일 경로 중 하나 이상으로 창을 식별한다.
- 마우스 좌표는 대상 창 client area 좌상단을 원점으로 하는 상대 좌표로 저장한다.
- 창 이동과 크기 변경 시 상대 좌표를 현재 화면 좌표로 다시 계산한다.
- 창이 최소화되면 자동으로 복원하고 활성화한 뒤 매크로를 계속 실행한다.
- 창이 사라진 경우에는 재탐색하고, 설정된 timeout 안에 찾지 못하면 매크로를 실패 처리한다.
- 창의 client area와 DPI 배율을 조회해 좌표를 보정한다.

### 5.3 액션 DSL과 순서 편집기

1차 액션 타입:

- `key`: 키 하나 또는 조합 입력
- `text`: 문자열 입력
- `mouse_move`: 창 상대 좌표로 이동
- `click`: 창 상대 좌표에서 왼쪽/오른쪽/중간 클릭
- `scroll`: 수직/수평 스크롤
- `wait`: 지정 시간 sleep
- `image_detect`: 이미지 탐지
- `image_wait`: 이미지가 나타날 때까지 대기
- `image_click`: 이미지를 찾아 클릭
- `retry`: 이미지 탐지 실패 시 지정 횟수와 간격으로 재시도
- `repeat`: 액션 목록 반복
- `condition`: 이미지 매칭 결과에 따른 분기
- `stop`: 현재 매크로 중단

액션은 기본적으로 순차 실행한다. 이미지 탐지 성공 후 `wait` 단계로 지정 시간 sleep한 다음 키보드 또는 마우스 입력을 실행할 수 있다. 각 단계는 중단 신호를 확인하고 timeout을 초과하면 매크로를 실패 상태로 종료한다. 이미지 탐지 단계는 실패 시 UI에서 설정한 재시도 횟수와 간격을 적용하며, 횟수를 모두 소진하면 다음 단계로 진행하지 않는다.

예시:

```json
{
  "type": "image_wait",
  "image": "images/confirm.png",
  "region": {"x": 100, "y": 80, "width": 500, "height": 300},
  "threshold": 0.9,
  "timeout_ms": 5000,
  "poll_interval_ms": 100,
  "retry_count": 3,
  "retry_interval_ms": 200
}
```

UI 순서 편집기는 단계 추가, 삭제, 복제, 위/아래 이동, 단계별 설정 편집을 제공한다. 특정 단계부터 테스트 실행하는 기능을 제공한다.

### 5.4 화면 인식과 해상도 대응

- 대상 창 client area 전체 또는 지정된 상대 좌표 영역을 캡처한다.
- 그레이스케일 템플릿 매칭을 사용한다.
- threshold, timeout, polling interval을 설정할 수 있다.
- 기준 이미지가 검색 영역보다 크면 즉시 실패한다.
- 창 크기와 DPI 배율 변화에 대응하기 위해 상대 좌표와 배율 보정을 사용한다.
- 배율 변화가 큰 경우에는 이미지 스케일 후보를 제한적으로 검색한다.
- 이미지 매칭 실패 시 score, 검색 영역, 현재 창 크기를 로그로 남긴다.

### 5.5 병렬 이미지 매칭 정책

- 한 매크로 안의 단계는 순서를 보장해야 하므로 기본적으로 순차 실행한다.
- 이미지 탐지를 포함해 하나의 매크로만 실행한다. 별도 실행 큐는 두지 않는다.
- 향후 병렬화는 실행 중인 매크로의 동일 프레임에 대한 템플릿 후보 계산에만 적용한다.
- 가능한 경우 하나의 캡처 프레임을 여러 이미지 matcher가 공유해 캡처 비용을 줄인다.
- CPU 사용량과 응답성을 위해 동시 매칭 작업 수에 상한을 둔다.
- 1차 구현은 순차 안정성을 우선하고, 병렬 매칭은 성능 측정 후 활성화한다.

### 5.6 입력과 실행 제어

- 앱이 백그라운드에 있어도 활성화된 매크로 시작 단축키를 감지한다.
- 일반 키보드/마우스 이벤트 기록은 수행하지 않는다.
- 매크로 입력은 대상 창의 상대 좌표와 현재 창 위치를 사용해 전송한다.
- 상태는 `STOPPED`, `ARMED`, `RUNNING`, `PAUSED`, `ERROR`로 관리한다.
- 한 매크로는 기본적으로 동시에 한 번만 실행한다.
- F8은 전체 일시정지/재개다.
- F9와 중지 버튼은 현재 액션, sleep, 반복, 이미지 검색을 즉시 중단한다.
- 앱 종료 시 단축키, 실행 큐, 타이머, native resource를 모두 정리한다.

### 5.7 UI

- 매크로 목록, 활성화 상태, 대상 창, 시작 단축키를 표시한다.
- 매크로 단계 카드를 순서대로 표시하고 편집한다.
- 실행 대상 창 선택과 창 상대 좌표 캡처 도구를 제공한다.
- 이미지 파일 선택, 검색 영역, threshold, timeout을 편집한다.
- 이미지 탐지 단계별 재시도 횟수와 재시도 간격을 편집한다.
- 현재 실행 중인 매크로와 단계를 표시한다.
- 성공, 중단, timeout, 오류 로그를 로컬에서 보여준다.
- renderer는 Node.js API와 파일 시스템에 직접 접근하지 않는다.

## 6. 저장과 import/export

- 매크로와 이미지는 Electron `app.getPath('userData')` 아래 앱 전용 폴더에 저장한다.
- 저장 데이터는 앱별 로컬 키로 암호화하고, 키는 Windows 보호 저장소를 우선 사용한다.
- 암호화 키 복구 실패에 대비해 export를 백업 수단으로 제공한다.
- export는 매크로 정의, 이미지, schema version, 무결성 정보를 하나의 패키지로 만든다.
- import는 압축 해제 경로 탈출과 허용되지 않은 파일 형식을 차단한다.
- 매크로 ID와 이미지 참조를 모두 검증한 뒤 저장한다.
- 저장소 schema version을 유지해 향후 마이그레이션을 지원한다.
- 사용자가 생성한 매크로와 이미지는 라이선스 만료만으로 삭제하지 않는다.

설정 예시:

```json
{
  "version": 2,
  "macros": [
    {
      "id": "confirm-flow",
      "name": "Confirm flow",
      "enabled": true,
      "hotkey": "ctrl+alt+q",
      "target_window": {
        "process_name": "example.exe",
        "title_contains": "Example"
      },
      "actions": []
    }
  ],
  "global": {
    "pause_hotkey": "f8",
    "stop_hotkey": "f9",
    "default_timeout_ms": 10000
  }
}
```

## 7. 오프라인 라이선스

- 제품은 인터넷 연결 없이 로컬 라이선스를 검증한다.
- 라이선스는 제품 공개키로 검증할 수 있는 서명된 파일이어야 한다.
- 라이선스에는 제품 ID, 발급 대상, 발급일, 만료일, 기능 등급, 장치 식별 정보가 포함된다.
- 사용자 요청에 따라 장치 바인딩 기준은 MAC 주소로 확정한다. MAC 주소의 제품별 SHA-256 해시와 Ed25519 서명을 함께 검증한다. MAC 변조 및 로컬 코드 수정까지 완전히 방지하지는 않는다.
- 라이선스 발급 도구의 개인키는 앱에 포함하지 않는다.
- 유효한 라이선스가 없으면 실제 입력을 수행하는 실행과 단계 테스트를 차단한다. 생성, 편집, 저장, import/export와 입력 없는 미리보기는 허용한다.
- 라이선스가 만료되는 즉시 새 매크로 실행을 차단한다.
- 만료된 상태에서도 저장된 매크로의 열람, 편집, export는 허용한다.
- 라이선스 만료 후 유예 기간은 두지 않는다.
- 라이선스 갱신 실패 시 사용자 데이터를 자동 삭제하지 않는다. 자동 삭제는 복구 불가능한 데이터 손실과 복구 지원 문제를 만들므로 기본 정책에서 제외한다.
- 라이선스 파일, 장치 정보, 매크로 데이터는 외부 서버로 전송하지 않는다.
- 라이선스 검증 로직은 우회될 수 있음을 전제로 하며, 제품 요구사항은 일반 사용자의 무단 사용 억제 수준으로 정의한다.

## 8. 보안과 권한

- Americano는 설치 과정 없이 실행한다. 실제 자동화 실행에 필요한 관리자 권한은 실행 앱에서 확인한다.
- 관리자 권한이 거부되면 자동화를 시작하지 않고 원인과 재실행 방법을 표시한다.
- 관리자 권한으로 실행된 대상 창에 입력하려면 앱도 같은 수준 이상의 권한이 필요하다.
- 키 입력 내용과 화면 캡처는 기본적으로 파일 로그에 저장하지 않는다.
- F9는 모든 매크로보다 우선해야 한다.
- 무한 반복에는 최대 반복 횟수나 명시적인 중단 조건이 있어야 한다.
- 입력 native module 오류, 캡처 실패, 창 소실은 UI에 표시하고 실행을 중단한다.
- preload는 허용된 IPC만 노출하고 renderer에 Node 통합을 허용하지 않는다.

## 9. 프로젝트 구조

```text
americano/
  electron/
    main.js
    preload.js
    index.html
    renderer.js
    styles.css
  src/
    config.js       # 매크로 스키마와 검증
    windows.js      # 창 탐색, 활성화, 좌표 변환
    capture.js      # 창 캡처와 영역 추출
    matcher.js      # 이미지 매칭
    input.js        # 키보드/마우스 입력
    actions.js      # 매크로 액션 실행기
    watcher.js      # 이미지 대기와 polling
    controller.js   # 매크로와 실행 상태 관리
  policy/center_patterns/
  targets/
  package.json
  config.example.json
```

## 10. 단계별 개발 계획

### Phase 1: 매크로 핵심

- 매크로 version 2와 앱 내부 저장소를 도입한다.
- wait, key, text, mouse_move, click을 구현한다.
- F8/F9, timeout, 중복 실행 방지를 완성한다.
- UI 순서 편집기와 매크로별 실행 버튼을 제공한다.

### Phase 2: 창 기반 자동화

- Windows native addon으로 대상 창 탐색, 활성화, 위치/크기를 추가한다.
- 창 상대 좌표와 DPI 배율 변환을 추가한다.
- image_detect, image_wait, image_click, condition을 구현한다.
- 관리자 권한 대상 창의 실행 조건과 안내를 구현한다.

### Phase 3: 병렬화와 편집 경험

- 동일 캡처 프레임 공유와 동시 매칭 상한을 추가한다.
- 단일 매크로 잠금을 유지하면서 템플릿 후보 계산의 병렬화를 측정한다.
- 화면 캡처 영역 선택, 매크로 복제, import/export를 제공한다.
- 실행 미리보기와 단계별 로그를 제공한다.

### Phase 4: 오프라인 라이선스와 배포

- 서명된 라이선스 파일 검증을 추가한다.
- 장치 바인딩과 유예 기간 없는 만료 정책을 추가한다.
- 만료 시 실행 차단과 사용자 데이터 보존 정책을 구현한다.
- native module Electron rebuild, Windows 권한, DPI, 다중 모니터, 포터블 앱 실행을 검증한다.

## 11. 제품 결정 항목

다음 항목의 구현 기준은 13절에서 확정한다.

1. 대상 창을 프로세스 이름, 창 제목, 실행 파일 경로 중 어떤 조합으로 식별할 것인가?
2. 해상도/DPI 변경 시 이미지 스케일 후보를 몇 단계까지 검색할 것인가?
3. 암호화 키를 Windows DPAPI에 저장하는 방식을 사용할 것인가?
4. 이미지 탐지 재시도 횟수의 기본값과 최대값은 얼마로 할 것인가?
5. 이미지 탐지 재시도 간격을 모든 단계에서 동일하게 할 것인가, 단계별로 다르게 할 것인가?
6. export 파일 자체도 암호화할 것인가?
7. 장치 바인딩은 사용자 요청에 따라 MAC 주소로 확정한다. MachineGuid/UUID 혼합 방식은 현재 사용하지 않는다.

## 12. 수용 기준

- Node.js 24에서 앱 빌드가 가능하고 결과 EXE는 Node.js 설치 없이 실행된다.
- 특정 앱 창을 매크로 대상으로 선택할 수 있다.
- Americano가 관리자 권한으로 실행되고 권한 실패 원인이 UI에 표시된다.
- 최소화된 대상 창이 자동 복원된 뒤 매크로가 계속 실행된다.
- 창 기준 상대 좌표로 키보드/마우스 입력이 동작한다.
- 관리자 권한 대상 창에 대한 권한 조건과 실패 원인이 UI에 표시된다.
- 이미지 탐지 후 sleep과 키보드 입력이 순서대로 실행된다.
- 창 크기와 DPI가 달라져도 상대 좌표와 이미지 탐지가 설정된 정책대로 동작한다.
- 여러 매크로를 등록하고 개별적으로 실행할 수 있다.
- 프로그램 전체에서 동시에 하나의 매크로만 실행되고 다른 실행 요청은 차단된다.
- 이미지 탐지를 포함해 단일 실행 잠금이 유지된다. 후보 병렬화는 성능 측정 후 별도 검증한다.
- 매크로와 이미지 import/export가 동작한다.
- 매크로와 이미지가 앱 내부 사용자 데이터 폴더에 암호화되어 저장된다.
- F8 일시정지와 F9 긴급 정지가 sleep, 반복, 이미지 검색보다 우선한다.
- 이미지 탐지 단계별 재시도 횟수와 간격을 UI에서 설정할 수 있다.
- 이미지 탐지 실패 시 설정된 횟수만큼 재시도한 후 매크로가 실패한다.
- 유효한 오프라인 라이선스가 없거나 만료되면 즉시 매크로 실행이 차단된다.
- Node.js 외 별도 언어 런타임과 AutoHotkey 엔진이 필요하지 않다.
- `build.bat`가 의존성을 설치하고 `dist/Americano-Portable.exe` 하나를 생성한다. 예제 컨피그와 사전 설정된 타깃을 배포하지 않으며 사용자가 UI에서 처음부터 구성한다.
- 설정은 Windows 사용자 데이터 폴더에 암호화 저장하고 EXE 옆에 컨피그 파일을 노출하지 않는다. EXE만 이동하면 설정까지 이동하지는 않는다. 기존 사용자 설정은 유지한다.

## 13. 확정한 구현 계약

### 실행과 취소

- enabled는 시작 단축키 등록 여부다. 앱 시작 시 매크로를 자동 실행하지 않는다. 수동 실행은 비활성 매크로도 허용한다.
- ARMED는 등록된 시작 단축키를 기다리는 상태, STOPPED는 단축키도 없는 상태다. 오류는 다음 실행까지 보존한다.
- 시작 요청 즉시 준비 작업보다 먼저 전역 잠금을 획득한다. 준비·일시정지·정리 중에도 다른 실행은 거부한다. 실행은 저장된 정의의 복사본과 고유 run ID를 사용한다.
- F8은 다음 입력과 대기 시간 진행을 멈춘다. timeout은 일시정지 시간을 제외한다. 재개 시 창/전경을 재확인한다.
- F9는 대기 타이머를 깨우고 취소를 전달한다. 이미 OS에 전달된 입력은 취소할 수 없다. 새 입력을 금지하고 눌린 키/버튼을 해제한 뒤 잠금을 해제한다.
- 취소 인지 목표는 100ms다. 취소 불가능한 native 작업은 종료 전까지 잠금을 유지한다. 매칭 계산은 worker 또는 협력적 분할로 main event loop의 장기 점유를 방지한다.
- F8/F9 등록 실패 시 실행을 차단한다. 앱에도 single-instance 잠금을 적용한다.

### 스키마와 액션

- version 2 ID는 1~64자의 영문/숫자/_/-이며 중복을 거부한다. 알 수 없는 version/type과 비유한 숫자는 거부한다.
- 단계 timeout 기본값은 10,000ms, 범위는 1~3,600,000ms다. wait.duration_ms는 0~3,600,000ms이며 지정 대기 시간 자체로 종료한다.
- key.keys는 ctrl+a 같은 조합, text.text는 최대 10,000자다. mouse_move/click의 x/y는 음수가 아닌 client 상대 좌표다. click.button은 left/right/middle이다.
- repeat는 count(1~10,000)와 actions를 갖는다. 중첩 깊이 8, 매크로당 저장된 액션 노드 1,000개로 제한한다. 무한 반복은 지원하지 않는다.
- image_detect는 한 번 검사하며 불일치는 실패다. image_wait는 timeout 동안 polling한다. image_click은 성공 위치 중심을 최신 창 위치로 변환한 후 클릭한다.
- 이미지 기본값: threshold 0.9, poll_interval_ms 100(30~60,000), retry_count 0(0~10), retry_interval_ms 200(0~60,000). 재시도는 단계별 설정이다.
- retry_count는 최초 시도 이후 추가 시도 횟수다. image_wait의 최악 실행 시간은 (retry_count + 1) * timeout_ms + retry_count * retry_interval_ms다. 권한·파일·스키마 오류는 재시도하지 않는다.
- retry는 이미지 액션 하나만 감싸며 자식의 retry_count와 중복 설정하지 않는다. 입력 액션을 자동 재시도하지 않는다.
- condition은 자체 이미지 검사 후 then/else 액션을 실행한다. 이전 실행의 매칭 결과를 재사용하지 않는다. stop은 오류가 아닌 중단으로 기록한다.
- 단축키는 대소문자·공백·modifier 순서·Control/Ctrl 별칭을 정규화해 충돌 검사한다. F8/F9는 예약한다. OS 등록 실패는 별도로 처리한다.
- version 1은 절대 화면 좌표/중앙 패턴 감시 모델이므로 자동 변환하지 않는다. 원본을 보존하고 대상 창과 순서를 다시 설정한다.

### 창과 DPI

- process_name/executable_path/title_contains 중 지정한 조건은 모두 일치해야 한다. 후보가 여러 개면 임의 선택하지 않고 재선택을 요구한다. HWND는 실행 세션에서만 보관한다.
- 좌표는 96 DPI 기준 DIP다. 매 입력 전 최신 client 원점과 DPI로 screen = origin + round(relative * dpi / 96)을 계산한다. client 크기에 따라 비례 확대하지 않는다. 범위 밖이면 실패한다.
- 최소화 창 복원과 전경 활성화를 확인한 후 입력한다. 전경 전환 실패 시 입력하지 않는다. 창 재탐색 timeout은 기본 10초다.
- 캡처는 실제 화면 client 영역이며 가림·잠금 화면·보호 콘텐츠는 지원하지 않는다. 다중 모니터 음수 화면 좌표를 허용한다.
- 이미지에 캡처 DPI를 저장하고 현재 DPI 비율의 0.9/1.0/1.1배 세 후보만 검색한다. 테마 변경과 임의 UI 재배치까지 자동 복구한다고 보장하지 않는다.

### 저장과 신뢰 경계

- 로컬 저장은 AES-256-GCM, 매 쓰기마다 새 12-byte nonce, 32-byte 키를 사용한다. 키는 Electron safeStorage의 Windows 보호 기능으로 암호화한다. 보호 기능이 없으면 평문으로 대체하지 않는다.
- 같은 디렉터리의 임시 파일을 원자적 rename으로 교체한다. 쓰기는 직렬화하고 복호화/검증 실패 시 기존 파일을 덮어쓰지 않는다. 이미지에도 동일한 보호 정책을 적용한다.
- export는 사용자가 선택하는 평문 이동용 패키지다. UI에 암호화되지 않음을 표시한다. SHA-256은 손상 검사용이며 발급자 인증 수단이 아니다. 이미지 상대 ID와 파일별/전체 크기 상한을 검증한다.
- 라이선스는 Ed25519로 정확한 payload 바이트를 검증한다. 개인키는 앱에 포함하지 않는다. 장치 바인딩은 정규화한 MAC 주소의 제품별 SHA-256 해시다. 등록된 MAC 중 하나라도 현재 PC의 어댑터와 일치하면 통과한다.
- UTC 만료 시각 이상이면 새 실행을 거부하고 실행 중 매크로도 다음 단계 경계에서 중단한다. 완전 오프라인에서 시계 조작의 완전한 방지는 보장하지 않는다.
- 앱에서 라이선스 파일을 선택하면 크기·서명·제품·MAC·기간·실행 기능 권한을 검증한 뒤 원자적으로 저장한다. 잘못된 갱신 파일은 기존 라이선스를 교체하지 않는다. 인증 상태는 UI에 표시하며 만료를 1초마다 확인하고 실행 중 대기/일시정지도 중단한다.
- 어댑터 주소는 Node.js 네트워크 정보와 Windows getmac.exe를 비동기로 조회한다. 인터넷은 필요하지 않으며 IP 미할당/연결 해제 어댑터도 조회한다. 두 조회 모두 실패하면 인증을 거부한다. 10초마다 다시 조회하고 수동 실행 권한 확인 시에도 갱신한다.
- 비교 빌드는 동일 검증기·공개키에 실제 MAC/가상 MAC의 30일 서명 라이선스를 각각 내장한다. 두 앱의 userData를 분리하고 일반 빌드에는 라이선스를 내장하지 않는다.
- IPC는 지정된 로컬 main frame과 명령 allowlist만 허용한다. main에서 입력을 재검증하고 임의 경로/코드를 받지 않는다. renderer에 CSP/sandbox/contextIsolation을 적용한다.
- 창 어댑터·라이선스·권한·긴급 단축키가 모두 준비되어야 실제 입력을 허용한다. 입력 없는 미리보기는 별도 모드로 표시한다.

### 단계별 검증 게이트

- Phase 1: version 2 저장/복원, 순서 편집, 미리보기, 중복 시작 거부, 긴 대기 중 정지, pause 시간 보존, 오류 보존, 손상된 저장소 보호를 자동 테스트한다. 실제 입력은 창/라이선스 어댑터 준비 전까지 연결하지 않는다.
- Phase 2: Windows 실기기로 최소화 복원, 전경 실패, 대상 종료/중복 후보, 100/125/150/200% DPI, 음수 화면 좌표, 이미지 timeout/재시도 후 입력 방지를 검증한다.
- Phase 3: import 경로 탈출·과대 파일·누락 이미지·ID 충돌·중간 실패 시 롤백을 검증한다.
- Phase 4: 서명 변조·장치 불일치·만료 경계·키 복구 실패 및 포터블 앱 native 모듈 로드를 검증한다. 공개키/서명 인증서와 Windows 실기기 검증 없이 배포 완료로 간주하지 않는다.

## 14. Americano Script 포맷

UOSteam의 `skill` 스크립트는 문법과 기능 설계를 참고하는 자료다. Americano는 `.uos` 파일을 호환하거나 실행하지 않으며, UOSteam 전용 게임 API를 포함하지 않는다. 대신 사용자가 조건·반복·대기·이벤트를 텍스트로 구성할 수 있는 자체 `Americano Script` 포맷을 제공한다.

### 14.1 포맷 계약

- 블록 문법은 `if/elseif/else/endif`, `while/endwhile`, `for/endfor`를 사용한다.
- 기본 명령은 `wait`, `image_wait`, `image_click`, `key`, `text`, `click`, `set`, `break`, `continue`, `stop`이다.
- 조건은 이미지 발견, 대상 창 상태, 이전 단계 결과, 변수 값과 `and/or/not` 조합을 지원한다.
- `timer`, `retry`, `timeout`, `poll_interval`은 모든 대기/탐지 명령에서 제한값과 함께 사용한다.
- 지원하지 않는 명령은 무시하지 않고 스크립트의 행·열을 포함한 오류로 표시한다.
- 스크립트는 원문과 검증된 AST를 매크로 데이터에 저장하며 renderer에서 임의 JavaScript로 실행하지 않는다.

예시:

```text
set attempts 0
while attempts < 3
  image_wait "confirm.png" timeout 5000
  click 420 310
  set attempts attempts + 1
endwhile
```

### 14.2 실행 안전성

- 스크립트는 기존 `MacroRunner`, 전역 실행 잠금, F8/F9 취소, 라이선스 검사를 공유한다.
- 모든 반복에는 최대 횟수와 취소 신호가 필요하며, 무한 반복과 무제한 대기는 허용하지 않는다.
- 화면 캡처, 창 탐색, 키보드/마우스 입력은 기존 main-process adapter만 사용한다.
- journal, item, target, gump, skill, mana 같은 UO 전용 상태는 제공하지 않는다. 필요한 일반 기능은 이미지/창/파일/실행 결과 이벤트로 표현한다.
- 종료 시 대기 타이머, 캡처 작업, native 입력 작업을 정리하고 정해진 deadline 안에 프로세스를 종료한다.

### 14.3 UI와 구현 순서

- Script 탭에서 포맷을 작성하고 행별 문법 오류를 표시한다.
- 블록 접기, 단계 미리보기, 현재 행 강조, 변수와 timer 상태를 제공한다.
- DSL-1: lexer/parser, 조건/반복/대기/중단과 위치 오류.
- DSL-2: 이미지/창/입력 built-in 연결과 retry/timeout.
- DSL-3: script와 기존 액션 카드 간 변환·검증, import/export.
- DSL-4: 실제 Windows 창·DPI·native 입력·종료 경로를 실기기에서 검증한다.

## 15. 프로세스별 캔버스 오버레이

- 대상 프로세스 이름·실행 경로·창 제목 조건을 AND로 적용한다. 여러 후보가 있으면 재선택을 요구한다.
- 매크로의 `overlay`는 client 영역의 96 DPI 기준 x/y/width/height다. 새 문서는 null이며 영역 설정 후 자동 저장한다.
- 저장된 테두리는 입력을 통과시키고 창의 현재 위치를 따라간다. 대상 종료 또는 영역 이탈 시 숨긴다.
- 오버레이 안에서 기준 이미지를 캡처하고 96 DPI로 정규화한다. 새 이미지 파일은 `.aimg` 암호화 자산으로 저장하며 미리보기와 참조는 암호화 매크로 문서에 저장한다.
- 오버레이가 지정된 매크로의 모든 이미지 검사는 저장 오버레이 전체를 검색한다. 조건은 매 반복마다 새 화면으로 평가하며 true/false 분기의 편집 가능한 단계 목록을 실행한다.
- `loop.count`는 1~10,000, `loop.interval_ms`는 30~60,000ms이며 기본은 1회/500ms다. 중간 정지하면 다음 반복을 시작하지 않는다. 무한 반복은 지원하지 않는다.
- F8/F9와 전역 실행 잠금을 공유한다. 매칭 계산은 협력적으로 양보하여 중지 이벤트 처리를 허용한다.
- 입력 없는 미리보기는 실제 탐지를 수행하지 않으며 false 분기를 미리보기한다.
