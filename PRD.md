# americano manager

## 1. 제품 개요

화면의 지정된 영역을 주기적으로 관찰하여 등록된 이미지를 찾고, 이미지별로 연결된 키보드 액션을 실행하는 개인용 자동화 도구다.

제품의 첫 사용 사례는 Windows 애플리케이션에서 동작하는 화면 기반 자동화이며, 설정과 탐지 엔진은 향후 macOS로 확장할 수 있도록 분리한다.

## 2. 목표와 비목표

### 목표

- 사용자가 화면 영역과 기준 이미지를 등록할 수 있다.
- 기준 이미지가 화면에 나타나면 지정된 키보드 액션을 한 번 실행한다.
- 같은 이미지가 계속 보이는 동안 액션이 반복 입력되지 않도록 한다.
- 탐지 신뢰도, 마지막 탐지 시각, 실행 결과를 확인할 수 있다.
- 설정을 파일로 저장하고 재실행 후 복원할 수 있다.
- 긴급 정지와 입력 차단 옵션을 제공해 오작동 피해를 줄인다.

### 비목표

- OCR, 얼굴/객체 인식, 자연어 명령은 MVP에 포함하지 않는다.
- 마우스 자동화와 복잡한 조건문은 MVP 이후로 미룬다.
- 로그인 우회, 보안 기능 우회, 게임 치팅 등 제3자 서비스의 이용약관을 위반하는 사용을 목표로 하지 않는다.

## 3. 플랫폼 및 실행 전략

### MVP: Windows 네이티브 실행

- Python 3.11 이상을 지원한다.
- 화면 캡처는 Windows 환경에서 검증된 캡처 라이브러리를 사용한다.
- 키보드 입력은 운영체제 수준의 입력 API를 사용한다.
- DPI 배율, 다중 모니터, 창 위치 변경을 고려한다.
- 전역 단축키를 등록한다. 기본값은 `F8` 일시정지/재개, `F9` 긴급 정지로 한다.

### 후속: macOS

- 화면 기록(Screen Recording)과 손쉬운 사용(Accessibility) 권한을 별도로 안내한다.
- 캡처·입력 구현을 `PlatformAdapter` 인터페이스 뒤에 둔다.
- Windows 전용 구현을 먼저 완성한 뒤 macOS 어댑터를 추가한다.

### Docker의 역할

Docker 컨테이너는 호스트 화면과 키보드에 안정적으로 접근하기 어렵기 때문에 MVP의 실제 자동화 실행 환경으로 사용하지 않는다. Docker는 다음 용도로만 제공한다.

- 설정 검증, 이미지 매칭 로직의 단위 테스트
- CI에서의 정적 검사와 테스트
- 향후 원격 분석 프로세스

## 4. 사용자 흐름

1. 프로그램을 실행한다.
2. 캡처할 모니터와 탐지 영역을 선택한다.
3. 화면에서 기준 이미지를 잘라 이름과 함께 저장한다.
4. 기준 이미지에 키보드 액션, 매칭 임계값, 재실행 정책을 지정한다.
5. 설정을 저장하고 감시를 시작한다.
6. 탐지 이벤트, 매칭 점수, 액션 실행 결과를 로그에서 확인한다.
7. `F8`로 일시정지하거나 `F9`로 즉시 모든 자동 입력을 차단한다.

## 5. 핵심 동작 설계

### 5.1 감시 파이프라인

```text
ScreenCapture -> RegionCrop -> TemplateMatch -> Debounce -> ActionExecutor -> EventLog
```

- `ScreenCapture`: 설정된 모니터의 화면을 캡처한다.
- `RegionCrop`: 전체 화면이 아니라 등록된 사각형 영역만 잘라 CPU 사용량을 줄인다.
- `TemplateMatch`: 기준 이미지와 현재 화면을 비교해 위치와 매칭 점수를 계산한다.
- `Debounce`: 연속 프레임 확인, 재실행 간격, 이미지 사라짐 조건을 적용한다.
- `ActionExecutor`: 허용된 키보드 액션을 순서대로 실행한다.
- `EventLog`: 탐지·무시·실행·실패 이벤트를 기록한다.

### 5.2 이미지 매칭

- MVP는 템플릿 매칭을 사용한다.
- 색상 정보가 필요하지 않은 화면 문구와 UI 패턴은 BGR 전체가 아니라 그레이스케일로 먼저 비교해 연산량을 줄인다.
- 그레이스케일 매칭이 임계값에 도달하지 못한 경우에만 Otsu 이진화 이미지를 보조 경로로 비교해 밝기·색상 변화에 대응한다.
- 두 경로 모두 기준 이미지와 화면 이미지의 크기를 확인하며, 기준 이미지가 영역보다 크면 즉시 불일치로 처리한다.
- 설정 가능한 값은 `threshold`, `scan_interval_ms`, `cooldown_ms`다.
- 기본 동작은 같은 대상이 화면에 계속 있는 동안 한 번만 실행하는 `on_appear`다.
- 대상이 일정 시간 사라진 뒤 다시 나타나야 다음 실행을 허용한다.
- 다중 스케일, 회전, 애니메이션 이미지는 MVP에서 제한하거나 별도 기준 이미지를 요구한다.
- 기준 이미지와 화면 캡처의 색상·해상도·DPI 차이로 인한 오탐은 로그에서 매칭 점수와 좌표를 제공해 진단한다.

### 5.2.1 중앙 허용 패턴 게이트

자동 입력 전에 화면 중앙 영역을 별도 캡처하고, 빌드에 포함된 허용 기준 이미지와 템플릿 매칭한다.

- 중앙 영역은 선택 모니터의 가로 25~75%, 세로 40~60%로 고정한다.
- 현재 빌드 고정 허용 패턴은 `니플레티`, `카시우스`다.
- 패턴 목록은 `policy/center_patterns.py`에 정의하며 `config.json`에서 변경할 수 없다.
- 기준 이미지는 `policy/center_patterns/<패턴명>.png`로 배포 빌드에 포함해야 한다.
- 허용 패턴 중 하나라도 `0.92` 이상으로 매칭되면 타깃 감시와 키보드 입력을 허용한다.
- 허용 패턴이 없거나 기준 이미지가 누락되면 모든 자동 입력을 차단한다.
- 문자 인식 엔진은 사용하지 않는다. 폰트·해상도·DPI·안티앨리어싱이 기준 이미지와 다르면 매칭되지 않을 수 있다.

### 5.3 키보드 액션

MVP에서 지원하는 액션은 다음으로 제한한다.

- 단일 키 누름: `enter`, `space`, `esc`, `a` 등
- 키 조합: `ctrl+s`, `shift+tab` 등
- 순차 입력: 액션 목록과 각 액션 사이의 지연 시간
- 액션별 최대 실행 시간과 실패 처리

허용되지 않은 키 이름은 설정 로드 단계에서 거부한다. 액션 실행 중에는 긴급 정지 상태를 매 단계 확인한다.

### 5.4 상태 모델

- `STOPPED`: 감시하지 않음
- `RUNNING`: 캡처와 매칭 수행
- `PAUSED`: 상태를 유지하되 자동 입력을 실행하지 않음
- `EMERGENCY_STOP`: 사용자가 명시적으로 해제하기 전까지 입력을 차단
- `ERROR`: 복구 가능한 오류를 표시하고 자동 입력을 중지

## 6. 설정 파일

사람이 읽고 수정할 수 있는 YAML 또는 JSON을 사용한다. 초기 구현은 JSON으로 시작해 표준 라이브러리만으로 검증 가능하게 한다.

예시:

```json
{
	"version": 1,
	"monitor": 1,
	"scan_interval_ms": 100,
	"targets": [
		{
			"id": "confirm-button",
			"image": "targets/confirm-button.png",
			"region": {"x": 100, "y": 200, "width": 400, "height": 300},
			"threshold": 0.9,
			"trigger": "on_appear",
			"cooldown_ms": 1000,
			"actions": [
				{"type": "key_press", "key": "enter", "delay_ms": 0}
			],
			"enabled": true
		}
	]
}
```

규칙:

- 설정 파일 버전을 포함하고 알 수 없는 버전은 실행 전에 거부한다.
- 이미지 경로는 설정 파일 기준 상대 경로로 해석한다.
- 영역 좌표가 화면 밖이거나 너비·높이가 0이면 저장하지 않는다.
- 임계값은 `0.0` 이상 `1.0` 이하, 검사 주기는 최소 `30ms`로 제한한다.
- 설정 변경은 실행 중 자동 반영하지 않고, 저장 후 재로드 또는 재시작 시 반영한다.

## 7. 권한, 안전, 오류 처리

- 시작 시 캡처 대상 모니터와 입력 권한 상태를 확인한다.
- 권한이 없으면 자동 입력을 시작하지 않고 해결 방법을 표시한다.
- 기본 상태는 `STOPPED`이며 명시적인 시작 동작이 필요하다.
- 긴급 정지는 모든 액션 큐를 비우고 이후 키 입력을 차단한다.
- 프로그램 종료 시 눌린 키가 남지 않도록 반드시 키 해제 정리를 수행한다.
- 창 포커스가 필요한 경우 현재 포커스 창을 임의로 변경하지 않는다. 포커스 정책은 별도 설정으로 둔다.
- 캡처 이미지와 로그에는 민감한 화면이 포함될 수 있으므로 기본적으로 외부 전송하지 않는다.
- 오류는 사용자에게 요약하고, 상세 원인과 스택 트레이스는 로컬 로그에 기록한다.
- 중앙 허용 패턴 검증 실패 시 이유와 상관없이 자동 입력을 시작하지 않는다.

## 8. 권장 모듈 구조

```text
americano/
	app.py                 # 애플리케이션 시작과 상태 전환
	config.py              # 설정 스키마, 로드, 검증
	capture/
		base.py              # ScreenCapture 인터페이스
		windows.py           # Windows 캡처 구현
	matching/
		template.py          # 그레이스케일/Otsu 보조 템플릿 매칭
	input/
		base.py              # KeyboardAdapter 인터페이스
		windows.py           # Windows 키보드 구현
	runtime/
		watcher.py           # 주기 실행과 debounce
		actions.py            # 액션 큐와 실행
	policy/
		center_patterns.py   # 빌드 고정 중앙 패턴 목록과 게이트
		center_patterns/     # 허용 패턴 PNG 번들
	.vscode/
		settings.json        # 검증된 Windows PowerShell 터미널 프로필
	build.bat              # Docker 이미지 빌드 및 테스트 실행
	Dockerfile             # 테스트 전용 Docker 이미지
	ui/                    # MVP 이후 설정 UI
	targets/               # 기준 이미지
	config.json
	tests/
```

핵심 로직은 캡처·입력 구현에 직접 의존하지 않고 인터페이스를 통해 접근한다. 따라서 실제 키 입력 없이 가짜 어댑터를 주입해 테스트할 수 있어야 한다.

## 9. MVP 수용 기준

- Windows에서 프로그램을 설치하고 실행할 수 있다.
- 하나의 모니터와 하나의 탐지 영역에 대해 기준 이미지를 등록할 수 있다.
- 이미지가 임계값 이상으로 감지되면 연결된 단일 키를 한 번 실행한다.
- 이미지가 계속 보이는 동안 중복 입력이 발생하지 않는다.
- 이미지가 사라졌다가 다시 나타나면 다시 실행된다.
- `F8` 일시정지와 `F9` 긴급 정지가 실제 입력보다 우선한다.
- 잘못된 설정, 이미지 누락, 캡처 권한 오류를 사용자에게 알리고 자동 입력을 시작하지 않는다.
- 매칭 점수, 탐지 시각, 실행 결과가 로컬 로그에 남는다.
- 캡처·매칭·debounce·설정 검증·액션 변환을 자동 테스트할 수 있다.
- 중앙 허용 패턴이 일치하지 않을 때 자동 입력이 차단된다.
- Docker를 통해 동일한 테스트 명령을 재현할 수 있다.

## 10. 단계별 개발 계획

### Phase 0: 실행 기반

- Python 환경과 Windows 실행 방법 확정
- 의존성 파일, 로깅, 설정 스키마 추가
- 캡처·입력 어댑터의 인터페이스와 가짜 구현 작성

### Phase 1: 단일 타깃 CLI

- 화면 영역을 설정 파일로 지정
- 템플릿 매칭과 단일 키 입력 구현
- `F8`/`F9`, cooldown, 로그, 테스트 구현
- 중앙 허용 패턴 게이트와 기준 이미지 번들 적용
- 그레이스케일 우선 및 Otsu 보조 매칭 적용

### Phase 2: 설정 UI

- 화면 선택과 영역 드래그
- 기준 이미지 캡처 및 미리보기
- 타깃별 액션 편집, 시작/정지, 상태 표시

### Phase 3: 안정화 및 macOS 준비

- 다중 모니터·DPI 테스트
- 성능 측정과 캡처 주기 최적화
- macOS 어댑터와 권한 안내 설계

## 11. 결정이 필요한 사항

- 1차 사용 대상 애플리케이션이 창 내부인지, 바탕화면 전체인지
- 대상 애플리케이션이 관리자 권한으로 실행될 때의 입력 권한 요구사항
- UI를 처음부터 제공할지, Phase 1을 CLI로 시작할지
- 템플릿 매칭 외에 색상/형태 변화 대응이 MVP에 필요한지
- 액션이 항상 현재 포커스 창에 전달되어야 하는지
- 설정과 로그를 저장할 기본 디렉터리 및 보존 기간

## 12. 현재 구현 상태

- 구현 완료: JSON 설정 검증, Windows 화면 캡처, 템플릿 매칭, cooldown, `on_appear` 중복 방지, 키보드 액션, F8/F9 제어
- 구현 완료: 그레이스케일 우선 매칭 및 임계값 미달 시 Otsu 이진화 보조 매칭
- 구현 완료: 중앙 허용 패턴 게이트. 현재 허용 패턴은 `니플레티`, `카시우스`
- 구현 완료: Docker 테스트 이미지와 `build.bat`
- 테스트 상태: 로컬 테스트 6개 통과
- 미완료: 실제 중앙 패턴 PNG 번들 추가, 설정 UI, macOS 어댑터